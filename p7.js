// Selecting the button using its ID
var button = document.getElementById('myButton');

// Adding a click event listener to the button

button.addEventListener('click', function() {
    alert('Button has been clicked!');
});
